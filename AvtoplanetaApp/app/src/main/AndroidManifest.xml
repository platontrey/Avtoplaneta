<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".AvtoplanetaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AvtoplanetaApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <!-- FileProvider для работы с камерой -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.avtoplaneta.avtoplanetaapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- MainActivity - главная активность с фрагментами -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- LoginActivity - экран входа -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- AddPartActivity - экран добавления запчасти -->
        <activity
            android:name=".AddPartActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- OrdersActivity - экран заказов -->
        <activity
            android:name=".OrdersActivity"
            android:exported="false" />

        <!-- CreateOrderActivity - экран создания заказа -->
        <activity
            android:name=".CreateOrderActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- SelectPartActivity - экран выбора запчасти -->
        <activity
            android:name=".SelectPartActivity"
            android:exported="false" />

        <!-- StatisticsActivity - экран статистики -->
        <activity
            android:name=".StatisticsActivity"
            android:exported="false" />

    </application>

</manifest>